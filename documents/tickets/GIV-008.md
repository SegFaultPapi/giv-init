# GIV-008: Setup wallet connection con RainbowKit

## 📋 Información del Ticket

**Epic:** Epic 0: Setup  
**Priority:** Alta  
**Estimate:** 2h  
**Status:** 🔄 TODO  
**Assignee:** -  

## 📖 User Story

**Como** usuario del dApp  
**Quiero** conectar mi wallet de forma fácil y segura  
**Para** interactuar con los contratos de donación en Base Sepolia

## 🎯 Acceptance Criteria

- [ ] Configurar RainbowKit con Base Sepolia
- [ ] Setup providers en _app.tsx
- [ ] Crear componente ConnectWallet
- [ ] Configurar wallets soportadas (MetaMask, Coinbase, WalletConnect)
- [ ] Verificar conexión funciona en Base Sepolia
- [ ] Mostrar balance y dirección del usuario

## 🔧 Technical Details

### Configuración de providers:
```typescript
// pages/_app.tsx
import { getDefaultWallets, RainbowKitProvider } from '@rainbow-me/rainbowkit';
import { configureChains, createConfig, WagmiConfig } from 'wagmi';
import { baseSepolia } from 'wagmi/chains';
import { publicProvider } from 'wagmi/providers/public';

const { chains, publicClient } = configureChains(
  [baseSepolia],
  [publicProvider()]
);

const { connectors } = getDefaultWallets({
  appName: 'GIV INIT',
  projectId: process.env.NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID!,
  chains
});

const wagmiConfig = createConfig({
  autoConnect: true,
  connectors,
  publicClient
});
```

### Componente ConnectWallet:
```typescript
// components/ConnectWallet.tsx
import { ConnectButton } from '@rainbow-me/rainbowkit';
import { useAccount, useBalance } from 'wagmi';

export const ConnectWallet = () => {
  const { address, isConnected } = useAccount();
  const { data: balance } = useBalance({ address });
  
  return (
    <div>
      <ConnectButton />
      {isConnected && (
        <div>
          <p>Address: {address}</p>
          <p>Balance: {balance?.formatted} ETH</p>
        </div>
      )}
    </div>
  );
};
```

### Styling con theme custom:
```typescript
// Integrar con el tema de shadcn/ui
const customTheme = merge(darkTheme(), {
  colors: {
    accentColor: '#0052FF', // giv-primary
  },
});
```

## 🔗 Dependencies

**Blocking:** GIV-002 (Base Sepolia), GIV-007 (shadcn/ui)  
**Blocks:** Ninguna (componente final)

## 📝 Definition of Done

- ✅ RainbowKit configurado con Base Sepolia
- ✅ Wallets principales soportados
- ✅ Componente ConnectWallet funcional
- ✅ Balance y dirección se muestran correctamente
- ✅ Styling consistente con el tema del proyecto
- ✅ Funciona en mobile y desktop

## 🚨 Notes

- Necesitas WalletConnect Project ID (gratis en walletconnect.com)
- Verificar que Base Sepolia tiene fondos de test disponibles
- El componente será usado en toda la aplicación

## 🔄 Related Issues

- Depende de GIV-002 (configuración de Base Sepolia)
- Usa el tema de GIV-007 (shadcn/ui)
- Componente central para toda la interacción del usuario