# GIV-004: Setup Morpho SDK en frontend

## 📋 Información del Ticket

**Epic:** Epic 0: Setup  
**Priority:** Media  
**Estimate:** 1.5h  
**Status:** 🔄 TODO  
**Assignee:** -  

## 📖 User Story

**Como** desarrollador frontend  
**Quiero** instalar el SDK de Morpho  
**Para** integrar con el vault de yield que genera retornos del 5% fee

## 🎯 Acceptance Criteria

- [ ] Instalar `@morpho-org/blue-sdk` en packages/nextjs
- [ ] Instalar `@morpho-org/morpho-ts` en packages/nextjs
- [ ] Verificar que se importan sin errores de TypeScript
- [ ] Configurar tipos en tsconfig.json si es necesario
- [ ] Crear wrapper básico para Morpho client

## 🔧 Technical Details

### Commands a ejecutar:
```bash
cd packages/nextjs
yarn add @morpho-org/blue-sdk @morpho-org/morpho-ts
```

### Configuración básica:
```typescript
// lib/morpho.ts
import { MorphoBlueSDK } from "@morpho-org/blue-sdk";
import { createPublicClient, http } from "viem";
import { baseSepolia } from "viem/chains";

const publicClient = createPublicClient({
  chain: baseSepolia,
  transport: http()
});

export const morphoClient = new MorphoBlueSDK({
  chain: baseSepolia,
  publicClient
});
```

### Test de integración básica:
```typescript
// Verificar que podemos hacer queries básicas
const vaultData = await morphoClient.getVault(vaultAddress);
```

## 🔗 Dependencies

**Blocking:** GIV-001 (Setup inicial Scaffold-ETH 2)  
**Blocks:** Ninguna (independiente)

## 📝 Definition of Done

- ✅ SDKs de Morpho instalados correctamente
- ✅ Imports funcionan sin errores TypeScript
- ✅ Wrapper básico para client creado
- ✅ Configuración para Base Sepolia
- ✅ Test de conexión básica pasa
- ✅ Documentación de integración actualizada

## 🚨 Notes

- Morpho Blue es la versión más reciente, usar esa
- Verificar que Morpho está deployado en Base Sepolia
- Los SDKs nos ayudarán a mostrar stats del vault en el dashboard

## 🔄 Related Issues

- Independiente de otros tickets de setup
- Será usado en el dashboard para mostrar yield stats
- Necesario para la integración con el vault ERC-4626