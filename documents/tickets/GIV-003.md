# GIV-003: Instalar dependencias Uniswap V4

## 📋 Información del Ticket

**Epic:** Epic 0: Setup  
**Priority:** Alta  
**Estimate:** 2h  
**Status:** 🔄 TODO  
**Assignee:** -  

## 📖 User Story

**Como** desarrollador de smart contracts  
**Quiero** instalar las dependencias de Uniswap V4  
**Para** implementar hooks personalizados que cobren el 5% fee en cada swap

## 🎯 Acceptance Criteria

- [ ] Instalar `Uniswap/v4-core` con forge install
- [ ] Instalar `Uniswap/v4-periphery` con forge install
- [ ] Verificar que las librerías se importan correctamente
- [ ] Configurar remappings en foundry.toml
- [ ] Compilar sin errores con `forge build`

## 🔧 Technical Details

### Commands a ejecutar:
```bash
cd packages/foundry
forge install Uniswap/v4-core
forge install Uniswap/v4-periphery
```

### Configuración en foundry.toml:
```toml
[profile.default]
src = "contracts"
out = "out"
libs = ["lib"]
remappings = [
    "@uniswap/v4-core/=lib/v4-core/",
    "@uniswap/v4-periphery/=lib/v4-periphery/",
    "@openzeppelin/contracts/=lib/openzeppelin-contracts/contracts/"
]
```

### Test de importación básica:
```solidity
import {IHooks} from "@uniswap/v4-core/src/interfaces/IHooks.sol";
import {BaseHook} from "@uniswap/v4-periphery/contracts/BaseHook.sol";
import {PoolManager} from "@uniswap/v4-core/src/PoolManager.sol";
```

## 🔗 Dependencies

**Blocking:** GIV-001 (Setup inicial Scaffold-ETH 2)  
**Blocks:** GIV-006 (Testing Setup)

## 📝 Definition of Done

- ✅ v4-core y v4-periphery instalados correctamente
- ✅ Remappings configurados en foundry.toml
- ✅ Imports de Uniswap funcionan sin errores
- ✅ `forge build` compila exitosamente
- ✅ Test básico de importación creado y pasa
- ✅ Documentación de dependencias actualizada

## 🚨 Notes

- Uniswap V4 aún está en desarrollo, usar la versión más estable disponible
- Los hooks son la feature clave que diferencia nuestro proyecto
- Verificar compatibilidad con Base Sepolia (V4 deployment addresses)

## 🔄 Related Issues

- Bloqueado por GIV-001 (necesita Foundry configurado)
- Prerequisito para implementar el hook con 5% fee
- Necesario para GIV-006 (testing del hook)